quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER}
quarkus.datasource.password=${DB_PASSWORD}
quarkus.datasource.jdbc.url=${DB_URL}
quarkus.hibernate-orm.database.generation=update


# =============================================================
# JWT ? HMAC HS256 (symmetric key)
# HAPUS mp.jwt.verify.publickey.location=NONE karena itu
# menyebabkan Quarkus skip verifikasi token ? semua JWT gagal!
# =============================================================
mp.jwt.verify.issuer=https://dishubosrm.acehprov.go.id
smallrye.jwt.sign.key=r7NmAExOMIt9qJiz0Hbyhf5f/lhUFcO8K4HhtSaqBHQ=
smallrye.jwt.verify.key=r7NmAExOMIt9qJiz0Hbyhf5f/lhUFcO8K4HhtSaqBHQ=
smallrye.jwt.verify.algorithm=HS256


app.base.url=https://dishubosrm.acehprov.go.id
app.frontend.url=https://seulamat.dishubaceh.com

# --- EMAIL ---
quarkus.mailer.host=smtp.gmail.com
quarkus.mailer.port=465
quarkus.mailer.ssl=true
quarkus.mailer.mock=false
quarkus.mailer.from=${MAIL_USERNAME}
quarkus.mailer.username=${MAIL_USERNAME}
quarkus.mailer.password=${MAIL_PASSWORD}

quarkus.http.host=0.0.0.0
quarkus.http.body.uploads-directory=/opt/mudik-v2/uploads
quarkus.http.limits.max-body-size=20M
quarkus.http.read-timeout=60s

# =============================================================
# CORS ? BUILT-IN QUARKUS
# =============================================================
quarkus.http.cors=true
quarkus.http.cors.origins=https://seulamat.dishubaceh.com
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS,HEAD,PATCH
quarkus.http.cors.headers=origin,content-type,accept,authorization,userid,x-requested-with
quarkus.http.cors.exposed-headers=content-disposition,content-type
quarkus.http.cors.access-control-allow-credentials=true
quarkus.http.cors.access-control-max-age=24H

# =============================================================
# AUTH PERMISSIONS
# =============================================================
# proactive=false: auth check hanya dilakukan jika endpoint membutuhkan
quarkus.http.auth.proactive=false

# FIX: Hapus rule public /api/* yang terlalu luas ? ini bertabrakan
# dengan rule authenticated /api/user/* dan bisa membuat Quarkus
# skip auth check sama sekali, atau sebaliknya menolak token valid.
# Ganti dengan permit hanya untuk path yang memang publik:
quarkus.http.auth.permission.public.paths=/api/auth/*,/api/rute,/api/portal/status,/api/feedback/publik,/api/feedback/stats,/api/peta/*
quarkus.http.auth.permission.public.policy=permit

# /api/user/* wajib token
quarkus.http.auth.permission.authenticated.paths=/api/user/*
quarkus.http.auth.permission.authenticated.policy=authenticated

# CORS FIX: Izinkan OPTIONS preflight untuk /api/user/* tanpa auth
quarkus.http.auth.permission.user-preflight.paths=/api/user/*
quarkus.http.auth.permission.user-preflight.methods=OPTIONS
quarkus.http.auth.permission.user-preflight.policy=permit

# CORS FIX: Izinkan OPTIONS preflight untuk /api/pendaftaran/* tanpa auth
# (Browser mengirim preflight OPTIONS sebelum POST/PUT dengan Authorization header)
quarkus.http.auth.permission.pendaftaran-preflight.paths=/api/pendaftaran/*
quarkus.http.auth.permission.pendaftaran-preflight.methods=OPTIONS
quarkus.http.auth.permission.pendaftaran-preflight.policy=permit

# CORS FIX: Izinkan OPTIONS preflight untuk /api/admin/* tanpa auth
quarkus.http.auth.permission.admin-preflight.paths=/api/admin/*
quarkus.http.auth.permission.admin-preflight.methods=OPTIONS
quarkus.http.auth.permission.admin-preflight.policy=permit

# CORS FIX: Izinkan OPTIONS preflight untuk semua /api/* lainnya
quarkus.http.auth.permission.api-preflight.paths=/api/*
quarkus.http.auth.permission.api-preflight.methods=OPTIONS
quarkus.http.auth.permission.api-preflight.policy=permit