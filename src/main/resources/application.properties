# --- DATABASE ---
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER}
quarkus.datasource.password=${DB_PASSWORD}
quarkus.datasource.jdbc.url=${DB_URL}
quarkus.hibernate-orm.database.generation=update

# --- KEAMANAN JWT ---
mp.jwt.verify.publickey.location=NONE
mp.jwt.verify.issuer=https://dishubosrm.acehprov.go.id
smallrye.jwt.sign.key=r7NmAExOMIt9qJiz0Hbyhf5f/lhUFcO8K4HhtSaqBHQ=
smallrye.jwt.verify.key=${smallrye.jwt.sign.key}
smallrye.jwt.verify.algorithm=HS256

# --- APP URL ---
app.base.url=https://dishubosrm.acehprov.go.id
app.frontend.url=https://seulamat.dishubaceh.com

# --- SERVER CONFIG ---
quarkus.http.host=0.0.0.0
quarkus.http.body.uploads-directory=/opt/mudik-v2/uploads
quarkus.http.limits.max-body-size=20M
# WAJIB TRUE BIAR TOKEN KEBACA MESKIPUN PUBLIC
quarkus.http.auth.proactive=true 



# Kita masukin SEMUA endpoint (termasuk user & secure) ke PUBLIC
# Biar browser gak kena blokir pas Preflight/Cek Ombak.
quarkus.http.auth.permission.public.paths=\
/api/auth/*,\
/api/rute,\
/api/rute/*,\
/api/pendaftaran/riwayat,\
/api/pendaftaran/uploads/*,\
/api/pendaftaran/uuid/*,\
/api/admin/users,\
/api/admin/users/*,\
/api/admin/pendaftar,\
/api/admin/verifikasi-keluarga/*,\
/api/admin/kendaraan,\
/api/admin/kendaraan/*,\
/api/admin/assign-bus,\
/api/admin/stats,\
/api/admin/export,\
/api/user/*,\
/api/secure/*

quarkus.http.auth.permission.public.policy=permit

# --- EMAIL CONFIG ---
quarkus.mailer.host=smtp.gmail.com
quarkus.mailer.port=465
quarkus.mailer.ssl=true
quarkus.mailer.mock=false
quarkus.mailer.from=${MAIL_USERNAME}
quarkus.mailer.username=${MAIL_USERNAME}
quarkus.mailer.password=${MAIL_PASSWORD}